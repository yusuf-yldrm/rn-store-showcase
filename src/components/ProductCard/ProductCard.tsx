import React from "react";
import { View } from "react-native";
import { InterBoldText } from "../StyledText";

const ProductCard = () => {
  return (
    <View>
      <InterBoldText>ProductCard</InterBoldText>
    </View>
  );
};

export default ProductCard;
