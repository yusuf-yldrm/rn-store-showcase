export type { Request } from "./types";

import product from "./product";

// i didnt find a name to a project i give the JAVASCRIPT STORE name
const jsStore = {
  product,
};

export default jsStore;
